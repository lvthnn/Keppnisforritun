#include <iostream>
#include <string>

using namespace std;

int main() {
  string input;
  cin >> input;
  int N = input.length();

  cout << input[0];
  for (int i = 1; i < N - 1; i++)
    if (input[i] == '-')
      cout << input[i + 1];

  return 0;
}
